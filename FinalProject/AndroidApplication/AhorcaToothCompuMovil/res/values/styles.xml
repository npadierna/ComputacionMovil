<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <!-- Keyboard Key Button -->
    <style name="KeyboardKey" parent="AppBaseTheme">
        <item name="android:background">@android:color/transparent</item>
        <item name="android:focusable">false</item>
        <item name="android:maxHeight">@dimen/keyboard_key_max_height</item>
        <item name="android:maxWidth">@dimen/keyboard_key_max_width</item>
        <item name="android:minHeight">@dimen/keyboard_key_min_height</item>
        <item name="android:minWidth">@dimen/keyboard_key_min_width</item>
        <item name="android:textColor">@color/keyboard_key_button_text_color</item>
        <item name="android:textStyle">bold</item>
    </style>

</resources>