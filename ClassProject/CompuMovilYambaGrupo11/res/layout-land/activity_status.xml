<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".StatusActivity" >

    <Button
        android:id="@+id/yambaButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="17dp"
        android:onClick="onLetsYamba"
        android:text="@string/tweet_text_button" >
    </Button>

    <EditText
        android:id="@+id/statusEditText"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_alignTop="@+id/yambaButton"
        android:layout_toLeftOf="@+id/yambaButton"
        android:ems="10"
        android:hint="@string/hint_status_edit_text"
        android:inputType="textMultiLine" >

        <requestFocus>
        </requestFocus>
    </EditText>

    <TextView
        android:id="@+id/counterTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/yambaButton"
        android:layout_below="@+id/yambaButton"
        android:layout_marginRight="17dp"
        android:text="@string/maximum_chars_text_view"
        android:textAppearance="?android:attr/textAppearanceSmall"
        android:textStyle="bold" >
    </TextView>

</RelativeLayout>